# CryptoMagic
 • Created an encrypting / decrypting utility that takes arguments from the command line and can either encrypt or
 decrypt a message.
 • Used C to conduct File I/O operations, reading and writing the encrypted or decrypted text to a file with the same
 base filename to a .crp or .txt file.
